- name: rails app log
  template:
    src: rails_app
    dest: /etc/logrotate.d/rails_app
    mode: 0644
    owner: root
    group: root

- name: clamav log
  template:
      src: clamav
      dest: /etc/logrotate.d/clamav
      mode: 0644
      owner: root
      group: root
