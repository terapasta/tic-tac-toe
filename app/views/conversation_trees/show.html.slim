= render layout: 'bots/layout' do
  span(
    data-component="ConversationTree"
    data-bot-id=@bot.id
    data-questions-tree=@question_answers.as_tree_json.to_json
    data-questions-repo=@question_answers.as_repo_json.to_json
    data-decision-branches-repo=DeepCamelizeKeys.deep_camelize_keys(@decision_branches.as_repo_json).to_json
  )

  #ConversationTree
  /
    data-answers-tree=@answers.as_tree_json.to_json
    data-answers-repo=@all_answers.as_repo_json.to_json
