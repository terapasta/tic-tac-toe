- if current_user.normal?
  .well
    = form_tag bot_imported_sentence_synonyms_path(@bot), class: 'form-inline', method: :get do
      = collection_select :filter, :worker_id, User.worker, :id, :email, { include_blank: true, prompt: '-- ワーカーを選択 --', selected: params.dig(:filter, :worker_id) }, class: 'form-control'
      = submit_tag '絞込', class: 'btn btn-default'


  - if params.dig(:filter, :worker_id).present?
    .well
      - total_count = @imported_training_messages.count
      - completed_count = @imported_training_messages.joins(:sentence_synonyms).where('sentence_synonyms.created_user_id' => params.dig(:filter, :worker_id)).count
      code #{ completed_count} / #{ @imported_training_messages.count }, 残り:#{ total_count - completed_count }

table.table.table-striped
  thead
    tr
      th id
      th トレーニングメッセージ
      th 同義文
  tbody
    - @imported_training_messages.each do |itm|
      - if current_user.normal? || policy_scope(itm.sentence_synonyms).any?
        tr
          th= itm.id
          th= itm.question
          th
            table.table.table-striped: tbody
              - ss_list = policy_scope(itm.sentence_synonyms)
              - ss_list = ss_list.select{ |ss| ss.created_user_id == params[:filter][:worker_id].to_i } if params.dig(:filter, :worker_id).present?
              - ss_list.each do |ss|
                tr
                  td= ss.id
                  td= ss.body
                  td= ss.created_user.email
                  th= link_to '削除', bot_imported_sentence_synonym_path(@bot, ss), class: 'btn btn-danger', data: { confirm: '本当に削除してよろしいですか？', method: :delete }
