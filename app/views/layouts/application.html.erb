<!DOCTYPE html>
<html>
<head>
  <title>My-ope office - 社内問い合わせ専用AIチャットボット</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%#= stylesheet_link_tag    'lumen', media: 'all' %>
  <%= stylesheet_link_tag    'cerulean', media: 'all' %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <% if Rails.env.production? %>
    <%= render 'layouts/shared/analytics/mixpanel' %>
  <% end %>
</head>
<body>
  <% if Rails.env.production? %>
    <%= render 'layouts/shared/analytics/google_analytics' %>
  <% end %>

  <% if params[:token].present? %>
    <%= render 'layouts/shared/ui/header_bot', bot: @bot %>
  <% else %>
    <%= render 'layouts/shared/ui/header' %>
  <% end %>

  <% if !chat_page? && policy(:static_pages).can_use_nav?(@bot) %>
    <div id="sidebar-wrapper" class="col-sm-2 col-xs-12">
      <div class="sidebar-nav list-group">
        <%= link_to edit_bot_path(@bot), class: 'bot-profile list-group-item text-center' do %>
          <p><%= @bot.name %></p>
          <% if @bot.image %>
            <%= image_tag @bot.image_url(:thumb) %>
          <% end %>

          <div id="bot-status-label">
            <%= render 'shared/learning_status_label', bot: @bot %>
          </div>
        <% end %>
        <%= link_to 'チャットする', new_chats_path(@bot.token), class: 'list-group-item', target: '_blank' %>
        <%= link_to "育てる", new_bot_training_path(@bot), class: 'list-group-item' %>
        <%= link_to "学習データをエクスポート", bot_exports_path(@bot), class: 'list-group-item' %>
        <%= link_to "学習データをインポート", new_bot_imports_path(@bot), class: 'list-group-item' %>
        <%= link_to "対話履歴", bot_threads_path(@bot), class: 'list-group-item' %>
        <%= link_to "Q&A管理", bot_question_answers_path(@bot), class: 'list-group-item' %>
        <%= link_to "回答", bot_answers_path(@bot), class: 'list-group-item' %>
        <%= link_to "会話ツリー", bot_conversation_tree_path(@bot), class: 'list-group-item' %>
      </div>
      <%= link_to "学習を実行する", bot_learning_path(@bot), method: :patch, remote: true, class: 'btn btn-danger btn-block' %>
    </div>
    <% container_main_class = 'col-sm-10 col-sm-offset-2 ' %>
  <% end %>

  <div class="container container-main <%= container_main_class %>">
    <%= render "shared/flash" %>
    <%= yield %>
  </div>
  <%= javascript_include_tag 'application' %>
</body>
</html>
